```c++
#include <string>
#include <cstring>
#include <iostream>
#include <fstream>
//#include <math.h>
using namespace std;
const int sup=320;
const int inf=160;
int str_to_int(const string &a){
    if(a.empty()) return 0;
    else if(a[0]=='-'){
        if(a.size()==1) return 0;
        else{
            int ans=0;
            for(unsigned int i=1;i<a.size();i++){ans*=10;ans+=int(a[i]-'0');}
            return ans;
        }
    }
    else{
        int ans=0;
        for(unsigned int i=0;i<a.size();i++){ans*=10;ans+=int(a[i]-'0');}
        return ans;
    }
}
struct element{
    char index[66];
    int value;
};
struct lead{
    long long int location;
    char index[66];
    int value;
    long long int last;
    long long int next;
    int block_size;
};
int Element_cmp(const element &x,const element &y){
    const int cmp=strcmp(x.index,y.index);
    if(cmp==0&&x.value==y.value) return 0;
    else if((cmp==0&&x.value<y.value)||cmp<0) return -1;
    else return 1;
}
int Lead_cmp(const lead &x,const lead &y){
    const int cmp=strcmp(x.index,y.index);
    if(cmp==0&&x.value==y.value) return 0;
    else if((cmp==0&&x.value<y.value)||cmp<0) return -1;
    else return 1;
}
int Element_Lead_cmp(const element &x,const lead &y){
    const int cmp=strcmp(x.index,y.index);
    if(cmp==0&&x.value==y.value) return 0;
    else if((cmp==0&&x.value<y.value)||cmp<0) return -1;
    else return 1;
}
//fstream element_all("file_element");//only for all the elements
//fstream deleted_block("file_block_deleted");//int deleted_count | long long int location...
//fstream lead_search("file_lead");//int element_count | int lead_count | long long int head_location | lead ...
//fstream deleted_lead("file_lead_deleted");//int delete_count | long long int loction...
fstream element_all;//only for all the elements
fstream deleted_block;//int deleted_count | long long int location...
fstream lead_search;//int element_count | int lead_count | long long int head_location | lead ...
fstream deleted_lead;//int delete_count | long long int loction...
long long int Locate_new_block(){//return a proper location for a new block
    deleted_block.clear();
    deleted_block.open("file_block_deleted");
    if(!deleted_block){
        deleted_block.open("file_block_deleted",ofstream::out);
        deleted_block.close();
        deleted_block.open("file_block_deleted");
    }
    deleted_block.seekg(0,ios::beg);
    int deleted_count=0;
    deleted_block.read(reinterpret_cast<char*>(&deleted_count),sizeof(int));
    deleted_block.close();
    if(!deleted_count){
        lead_search.clear();
        lead_search.open("file_lead");
        if(!lead_search){
            lead_search.open("file_lead",ofstream::out);
            lead_search.close();
            lead_search.open("file_lead");
        }
        lead_search.seekg(sizeof(int),ios::beg);
        int lead_count=0;
        lead_search.read(reinterpret_cast<char*>(&lead_count),sizeof(int));
        lead_search.close();
        long long int ans;
        ans=325*lead_count*sizeof(element);
        return ans;
    }
    else{
        deleted_block.clear();
        deleted_block.open("file_block_deleted");
        if(!deleted_block){
            deleted_block.open("file_block_deleted",ofstream::out);
            deleted_block.close();
            deleted_block.open("file_block_deleted");
        }
        long long int ans=0;
        deleted_block.seekg(sizeof(int)+(deleted_count-1)*sizeof(long long int),ios::beg);
        deleted_block.read(reinterpret_cast<char*>(&ans),sizeof(long long int));
        long long int p=0;
        deleted_block.seekp(sizeof(int)+(deleted_count-1)*sizeof(long long int),ios::beg);
        deleted_block.write(reinterpret_cast<char*>(&p),sizeof(long long int));
        --deleted_count;
        deleted_block.seekp(0,ios::beg);
        deleted_block.write(reinterpret_cast<char*>(&deleted_count),sizeof(int));
        deleted_block.close();
        return ans;
    }
}
long long int Locate_new_lead(){
    long long int ans=0;
    deleted_lead.clear();
    deleted_lead.open("file_lead_deleted");
    if(!deleted_lead){
        deleted_lead.open("file_lead_deleted",ofstream::out);
        deleted_lead.close();
        deleted_lead.open("file_lead_deleted");
    }
    int deleted_count;
    deleted_lead.seekg(0,ios::beg);
    deleted_lead.read(reinterpret_cast<char*>(&deleted_count),sizeof(int));
    if(!deleted_count){
        deleted_lead.close();
        lead_search.clear();
        lead_search.open("file_lead");
        if(!lead_search){
            lead_search.open("file_lead",ofstream::out);
            lead_search.close();
            lead_search.open("file_lead");
        }
        int lead_count=0;
        lead_search.seekg(sizeof(int),ios::beg);
        lead_search.read(reinterpret_cast<char*>(&lead_count),sizeof(int));
        ans=2*sizeof(int)+sizeof(long long int)+lead_count*sizeof(lead);
        lead_search.close();
    }
    else{
        deleted_lead.seekg(sizeof(int)+(deleted_count-1)*sizeof(long long int),ios::beg);
        deleted_lead.read(reinterpret_cast<char*>(&ans),sizeof(long long int));
        deleted_count--;
        deleted_lead.seekp(0,ios::beg);
        deleted_lead.write(reinterpret_cast<char*>(&deleted_count),sizeof(int));
        deleted_lead.close();
    }
    return ans;
}
void Insert(const char* index,const int &value){
    lead_search.clear();
    lead_search.open("file_lead");
    if(!lead_search){
        lead_search.open("file_lead",ofstream::out);
        lead_search.close();
        lead_search.open("file_lead");
    }
    lead_search.seekg(0,ios::beg);
    int m;
    lead_search.read(reinterpret_cast<char*>(&m),sizeof(int));
    lead_search.close();
    if(m==0) {
        element new_ele;
        strcpy(new_ele.index, index);
        new_ele.value = value;
        element_all.clear();
        element_all.open("file_element");
        if(!element_all){
            element_all.open("file_element",ofstream::out);
            element_all.close();
            element_all.open("file_element");
        }
        element_all.seekp(325 * sizeof(element),ios::beg);
        element_all.write(reinterpret_cast<char *>(&new_ele), sizeof(element));
        element_all.close();
        lead new_lead;
        new_lead.block_size = 1;
        strcpy(new_lead.index, index);
        new_lead.last = 2 * sizeof(int) + sizeof(long long int);
        new_lead.location = 325 * sizeof(element);
        new_lead.next = 2 * sizeof(int) + sizeof(long long int);
        new_lead.value = value;//finish the assignment
        lead_search.clear();
        lead_search.open("file_lead");
        if(!lead_search){
            lead_search.open("file_lead",ofstream::out);
            lead_search.close();
            lead_search.open("file_lead");
        }
        long long int loc = 2 * sizeof(int) + sizeof(long long int);
        lead_search.seekp(2 * sizeof(int),ios::beg);
        lead_search.write(reinterpret_cast<char *>(&loc), sizeof(long long int));
        lead_search.seekp(2 * sizeof(int) + sizeof(long long int) + sizeof(lead),ios::beg);
        lead_search.write(reinterpret_cast<char *>(&new_lead), sizeof(lead));
        int lead_count = 2;
        lead_search.seekp(sizeof(int),ios::beg);
        lead_search.write(reinterpret_cast<char *>(&lead_count), sizeof(int));
        lead_search.seekp(0,ios::beg);
        int element_count = 1;
        lead_search.write(reinterpret_cast<char *>(&element_count), sizeof(int));
        lead_search.close();
        return;
    }
    else{
        element new_ele;
        strcpy(new_ele.index,index);
        new_ele.value=value;//finish the assignment
        lead_search.clear();
        lead_search.open("file_lead");
        if(!lead_search){
            lead_search.open("file_lead",ofstream::out);
            lead_search.close();
            lead_search.open("file_lead");
        }
        element_all.clear();
        element_all.open("file_element");
        if(!element_all){
            element_all.open("file_element",ofstream::out);
            element_all.close();
            element_all.open("file_element");
        }
        int lead_count;
        lead_search.seekg(sizeof(int));
        lead_search.read(reinterpret_cast<char*>(&lead_count),sizeof(int));
        long long int loc;
        lead_search.seekg(2*sizeof(int));
        lead_search.read(reinterpret_cast<char*>(&loc),sizeof(long long int));
        lead test_lead;
        for(int i=0;i<lead_count;++i){//search the lead
            lead_search.seekg(loc,ios::beg);
            lead_search.read(reinterpret_cast<char*>(&test_lead),sizeof(lead));
            int cmp=Element_Lead_cmp(new_ele,test_lead);
            if(cmp==0){lead_search.close();return;}
            else if(cmp<0){
                if(i==0){lead_search.close();return;}
                else{
                    loc=test_lead.last;
                    lead_search.seekg(loc,ios::beg);
                    lead_search.read(reinterpret_cast<char*>(&test_lead),sizeof(lead));
                    //lead_search.close();//Now I close it.
                    const long long int all_loc=test_lead.location;//to file_element
                    const int block_size=test_lead.block_size;
                    element test_element;
                    for(int j=0;j<block_size;++j){
                        element_all.seekg(all_loc+j*sizeof(element),ios::beg);
                        element_all.read(reinterpret_cast<char*>(&test_element),sizeof(element));
                        int element_cmp=Element_cmp(new_ele,test_element);
                        if(element_cmp==0){element_all.close();lead_search.close();return;}
                        else if(element_cmp<0){
                            if(j==0){element_all.close();lead_search.close();return;}
                            else{
                                int move=j;//will insert here
                                element move_element;
                                for(int k=block_size-1;k>=j;--k){
                                    element_all.seekg(all_loc+k*sizeof(element),ios::beg);
                                    element_all.read(reinterpret_cast<char*>(&move_element),sizeof(element));
                                    element_all.seekp(all_loc+(k+1)*sizeof(element),ios::beg);
                                    element_all.write(reinterpret_cast<char*>(&move_element),sizeof(element));
                                }
                                element_all.seekp(all_loc+(move)*sizeof(element),ios::beg);
                                element_all.write(reinterpret_cast<char*>(&new_ele),sizeof(element));
                                //element_all.close();//Now I close it.
                                test_lead.block_size++;//change here
                                if(test_lead.block_size<sup){
                                    //lead_search.open("file_lead");
                                    int element_count=0;
                                    lead_search.seekg(0,ios::beg);
                                    lead_search.read(reinterpret_cast<char*>(&element_count),sizeof(int));
                                    element_count++;
                                    lead_search.seekp(0,ios::beg);
                                    lead_search.write(reinterpret_cast<char*>(&element_count),sizeof(int));
                                    lead_search.seekp(loc,ios::beg);
                                    lead_search.write(reinterpret_cast<char*>(&test_lead),sizeof(lead));
                                    lead_search.close();
                                    element_all.close();
                                    return;
                                }
                                else{
                                    const int mid=sup/2;
                                    const long long int new_block_loc=Locate_new_block();
                                    //element_all.open("file_element");
                                    for(int k=mid;k<test_lead.block_size;k++){
                                        element_all.seekg(all_loc+k*sizeof(element),ios::beg);
                                        element_all.read(reinterpret_cast<char*>(&move_element),sizeof(element));
                                        element_all.seekp(new_block_loc+(k-mid)*sizeof(element),ios::beg);
                                        element_all.write(reinterpret_cast<char*>(&move_element),sizeof(element));
                                    }
                                    element_all.seekg(new_block_loc,ios::beg);
                                    element_all.read(reinterpret_cast<char*>(&move_element),sizeof(element));
                                    //element_all.close();
                                    lead new_lead;
                                    new_lead.block_size=test_lead.block_size-mid;
                                    strcpy(new_lead.index,move_element.index);
                                    new_lead.location=new_block_loc;
                                    new_lead.value=move_element.value;
                                    new_lead.last=all_loc;
                                    new_lead.next=test_lead.next;
                                    const long long int new_lead_loc=Locate_new_lead();
                                    //lead_search.open("file_lead");
                                    int element_count;
                                    lead_search.seekg(0,ios::beg);
                                    lead_search.read(reinterpret_cast<char*>(&element_count),sizeof(int));
                                    element_count++;
                                    lead_search.seekp(0,ios::beg);
                                    lead_search.write(reinterpret_cast<char*>(&element_count),sizeof(int));
                                    lead_search.seekp(new_lead_loc,ios::beg);
                                    lead_search.write(reinterpret_cast<char*>(&new_lead),sizeof(lead));
                                    test_lead.block_size=mid;
                                    test_lead.next=new_lead_loc;
                                    lead_search.seekp(loc,ios::beg);
                                    lead_search.write(reinterpret_cast<char*>(&test_lead),sizeof(lead));
                                    lead_search.seekg(test_lead.next,ios::beg);
                                    lead_search.read(reinterpret_cast<char*>(&new_lead),sizeof(lead));
                                    new_lead.last=new_lead_loc;
                                    lead_search.seekp(test_lead.next,ios::beg);
                                    lead_search.write(reinterpret_cast<char*>(&new_lead),sizeof(lead));
                                    int lead_count;
                                    lead_search.seekg(sizeof(int));
                                    lead_search.read(reinterpret_cast<char*>(&lead_count),sizeof(int));
                                    lead_count++;
                                    lead_search.seekp(sizeof(int),ios::beg);
                                    lead_search.write(reinterpret_cast<char*>(&lead_count),sizeof(int));
                                    lead_search.close();
                                    element_all.close();
                                    return;
                                }
                            }
                        }
                    }
                    //here
                }
            }
            else{
                loc=test_lead.next;
                continue;
            }
        }
        lead_search.close();
        element_all.close();
        return;
    }
}
void Delete(const char* index,const int &value){
    element targeted_element;
    strcpy(targeted_element.index,index);
    targeted_element.value=value;
    lead_search.clear();
    lead_search.open("file_lead");
    if(!lead_search){
        lead_search.open("file_lead",ofstream::out);
        lead_search.close();
        lead_search.open("file_lead");
    }
    int lead_count;
    lead_search.seekg(sizeof(int));
    lead_search.read(reinterpret_cast<char*>(&lead_count),sizeof(int));
    long long int lead_loc;
    lead_search.seekg(2*sizeof(int));
    lead_search.read(reinterpret_cast<char*>(&lead_loc),sizeof(long long int));
    lead test_lead;
    int lead_cmp=0;
    element_all.clear();
    element_all.open("file_element");
    if(!element_all){
        element_all.open("file_element",ofstream::out);
        element_all.close();
        element_all.open("file_element");
    }
    for(int i=0;i<lead_count;++i) {
        lead_search.seekg(lead_loc,ios::beg);
        lead_search.read(reinterpret_cast<char *>(&test_lead), sizeof(lead));
        lead_cmp = Element_Lead_cmp(targeted_element, test_lead);
        if (lead_cmp < 0) {
            element_all.close();
            lead_search.close();
            return;
        } else if (lead_cmp > 0) {
            const long long int all_loc = test_lead.location;
            const int block_size = test_lead.block_size;
            element test_element;
            int element_cmp = 0;
            for (int j = 0; j < block_size; ++j) {
                element_all.seekg(all_loc + j * sizeof(element),ios::beg);
                element_all.read(reinterpret_cast<char *>(&test_element), sizeof(element));
                element_cmp = Element_cmp(test_element, targeted_element);
                if (element_cmp == 0) {
                    int move = j;
                    element move_element;
                    for(int k=j+1;k<block_size;k++) {
                        element_all.seekg(all_loc + k * sizeof(element), ios::beg);
                        element_all.read(reinterpret_cast<char *>(&move_element), sizeof(element));
                        element_all.seekp(all_loc + (k - 1) * sizeof(element), ios::beg);
                        element_all.write(reinterpret_cast<char *>(&move_element), sizeof(element));
                    }
                    test_lead.block_size--;

                } else if (element_cmp > 0) {
                    element_all.close();
                    lead_search.close();
                    return;
                }
            }
            lead_loc = test_lead.next;
        } else {

            element_all.close();
            lead_search.close();
            return;
        }
    }
}
void Find(const char* index){
    lead_search.clear();
    lead_search.open("file_lead");
    if(!lead_search){
        lead_search.open("file_lead",ofstream::out);
        lead_search.close();
        lead_search.open("file_lead");
    }
    element_all.clear();
    element_all.open("file_element");
    if(!element_all){
        element_all.open("file_element",ofstream::out);
        element_all.close();
        element_all.open("file_element");
    }
    int lead_count;
    lead_search.seekg(sizeof(int),ios::beg);
    lead_search.read(reinterpret_cast<char*>(&lead_count),sizeof(int));
    long long int loc;
    lead_search.seekg(2*sizeof(int),ios::beg);
    lead_search.read(reinterpret_cast<char*>(&loc),sizeof(long long int));
    int index_cmp=0;
    int count_out=0;
    lead test_lead;
    for(int i=0;i<lead_count;++i){
        lead_search.seekg(loc,ios::beg);
        lead_search.read(reinterpret_cast<char*>(&test_lead),sizeof(lead));
        index_cmp=strcmp(test_lead.index,index);
        if(index_cmp<=0) {
            long long int loc_all = test_lead.location;
            int block_size = test_lead.block_size;
            element test_element;
            for (int j = 0; j < block_size; ++j) {
                element_all.seekg(loc_all + j * sizeof(element), ios::beg);
                element_all.read(reinterpret_cast<char *>(&test_element), sizeof(element));
                index_cmp = strcmp(test_element.index, index);
                if (index_cmp == 0) {
                    cout << test_element.value << " ";
                    count_out++;
                }
            }
        }
        else break;
        loc = test_lead.next;
    }
    element_all.close();
    lead_search.close();
    if(!count_out) cout << "null" << endl;
    else cout << endl;
}
void Initialize(){
    lead_search.clear();
    lead_search.open("file_lead");
    if(!lead_search){
        lead_search.open("file_lead",ofstream::out);
        lead_search.close();
        lead_search.open("file_lead");
    }
    int m=0;
    lead_search.seekg(0,ios::beg);
    lead_search.read(reinterpret_cast<char*>(&m),sizeof(int));
    lead_search.close();
    if(m>0) return;
    else {
        lead_search.clear();
        lead_search.open("file_lead");
        if(!lead_search){
            lead_search.open("file_lead",ofstream::out);
            lead_search.close();
            lead_search.open("file_lead");
        }
        m = 0;
        lead_search.seekp(0,ios::beg);
        lead_search.write(reinterpret_cast<char *>(&m), sizeof(int));
        m = 1;
        lead_search.seekp(sizeof(int),ios::beg);
        lead_search.write(reinterpret_cast<char *>(&m), sizeof(int));
        long long int head_location = 2 * sizeof(int) + sizeof(long long int);
        lead_search.seekp(2 * sizeof(int),ios::beg);
        lead_search.write(reinterpret_cast<char *>(&head_location), sizeof(long long int));
        lead min;
        min.block_size = 1;
        memset(min.index, ' ', 66);
        min.last = head_location;
        min.location = 0;
        min.next = head_location;
        min.value = -2147483648;
        lead_search.seekp(head_location,ios::beg);
        lead_search.write(reinterpret_cast<char *>(&min), sizeof(lead));
        lead_search.close();
        element_all.clear();
        element_all.open("file_element");
        if(!element_all){
            element_all.open("file_element",ofstream::out);
            element_all.close();
            element_all.open("file_element");
        }
        element min_ele;
        memset(min_ele.index, ' ', 66);
        min_ele.value = min.value;
        element_all.seekp(0,ios::beg);
        element_all.write(reinterpret_cast<char *>(&min_ele), sizeof(element));
        element_all.close();
        return;
    }
}
int main(){
    Initialize();
    int n;
    cin >> n;
    string op;
    getchar();
    for(int i=0;i<n;i++){
        getline(cin,op);
        const unsigned long int l=op.size();
        unsigned int h,t;
        if(op[0]=='i'){
            h=6;
            while(op[h]==' '||op[h]=='[') h++;
            t=h;
            while(op[t]!=' '&&op[t]!=']'&&t<l) t++;
            string a=op.substr(h,t-h);
            h=t;
            char index[66];
            for(int i=0;i<a.size();i++) index[i]=a[i];
            while(op[h]==' '||op[h]=='[') h++;
            t=h;
            while(op[t]!=' '&&op[t]!=']'&&t<l) t++;
            string b=op.substr(h,t-h);
            h=t;
            Insert(index,str_to_int(b));
            continue;
        }
        if(op[0]=='d'){
            h=6;
            while(op[h]==' '||op[h]=='[') h++;
            t=h;
            while(op[t]!=' '&&op[t]!=']'&&t<l) t++;
            string a=op.substr(h,t-h);
            h=t;
            char index[66];
            for(int i=0;i<a.size();i++) index[i]=a[i];
            while(op[h]==' '||op[h]=='[') h++;
            t=h;
            while(op[t]!=' '&&op[t]!=']'&&t<l) t++;
            string b=op.substr(h,t-h);
            h=t;
            Delete(index,str_to_int(b));
            continue;
        }
        if(op[0]=='f'){
            h=5;
            while(op[h]==' '||op[h]=='[') h++;
            t=h;
            while(op[t]!=' '&&op[t]!=']'&&t<l) t++;
            string a=op.substr(h,t-h);
            h=t;
            char index[66];
            for(int i=0;i<a.size();i++) index[i]=a[i];
            Find(index);
            continue;
        }
    }
    return 0;
}
```

